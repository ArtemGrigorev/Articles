services:
 crud_api:
  build:
   context: .
   dockerfile: Dockerfile
  ports:
   - "8080:8080"
  container_name: tag_api
  depends_on:
   - db
 db:
   image: postgres:latest
   ports:
    - "5433:5433"
   environment:
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: qwerty
   container_name: db_postgre_conteiner

